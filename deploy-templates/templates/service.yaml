apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-petclinic-github1.fullname" . }}
  labels:
    {{- include "spring-petclinic-github1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-petclinic-github1.selectorLabels" . | nindent 4 }}
