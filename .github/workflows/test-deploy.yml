name: Test Deployment

on:
  push:
    branches:
      - master
      
jobs:
  Build:
    runs-on: ubuntu-latest
    environment:
      name: Build
    steps:
      - name: Checkout
        uses: actions/checkout@v3  
      - name: Build
        run: echo "Building..."
  
  Test:
    runs-on: ubuntu-latest
    environment:
      name: Test
    steps:
      - name: Test
        run: echo "Testing..."
  
  Deploy:
    runs-on: ubuntu-latest
    environment:
      name: Deploy

    steps:  
      - name: Deploy
        run: echo "Deploying..."
