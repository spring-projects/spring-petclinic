pipeline {
    agent {
        node {
            label 'linux-agent'
        }
    }
    stages {
        stage('Clone') {
            steps {
                echo 'Cloning' 
                git url: 'https://github.com/RolandBakunts/spring-petclinic.git'
                // withGradle(){
                // }
            }
        }
        stage('Build') {
            steps {
                echo 'Hello hgfds world!' 
                sh './gradlew task --all'
                // withGradle(){
                // }
            }
        }
    }
}