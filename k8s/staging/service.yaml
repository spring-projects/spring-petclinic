apiVersion: v1
kind: Service
metadata:
  namespace: staging
  name: spring-petclinic-plain
  labels:
    app: spring-petclinic-plain
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 9093
      targetPort: http
  selector:
    app: spring-petclinic-plain
