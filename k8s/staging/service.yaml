apiVersion: v1
kind: Service
metadata:
  namespace: staging
  name: spring-petclinic-plain
  labels:
    app: spring-petclinic-plain
spec:
  type: LoadBalancer
  ports:
    - port: 9093
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: spring-petclinic-plain
