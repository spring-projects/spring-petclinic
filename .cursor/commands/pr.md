# pr

Your job is to create a PR w/ a descriptive title. Always use the Github CLI. Add all untrackd files. If you haven't already made a commit, do that first with a clear commit history. Confirm after each step (add, commit, push).