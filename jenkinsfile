pipeline {
    agent any 

        stages{
            stage('build'){
               steps{
                    echo 'building'
                    sh 'mvnw -B -DskipTests clean package'
               }
            }
        }
}