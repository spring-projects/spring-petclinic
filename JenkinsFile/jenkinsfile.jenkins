pipeline {
    agent any

    tools {
        maven 'Maven 3.8.3'
    }

    stages {
        stage('Build&Test') {
            steps {
                sh 'mvn clean'
                sh 'mvn package'
            }
        }
    }
}
