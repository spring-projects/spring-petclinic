pipeline {
    agent any
    
    tools {
        maven 'Maven 3.2.5'
    }

    stages {
        stage('Build&Test') {
            steps {
                //dir ('${JENKINS_HOME}/workspace/javaProject') {}
                sh 'chmod +x ./build.sh'
                //sh './build.sh'
                sh '${JENKINS_HOME}/workspace/javaProject/build.sh'
            }
        }
    }
}
