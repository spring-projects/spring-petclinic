pipeline {
    agent any

    stages {
        stage('Build&Test') {
            steps {
                dir '${JENKINS_HOME}/workspace/javaProject'
                sh 'chmod +x ./build.sh'
                sh './build.sh'
            }
        }
    }
}
