pipeline {
    agent any
    stages{
        stage('build'){
            steps{
                echo "this is a build stage from test branch"
                echo "this is a build stage from test branch -2"
            }
        }
    }
}
