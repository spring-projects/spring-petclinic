pipeline {
  agent any

  
  stages {
    stage('Some step...') {
      steps {

        sh "echo ${REVIEW_STATE}"
        sh "echo ${REPO_NAME}"
        sh "echo ${PR_URL}"

      }
    }
  }
}








