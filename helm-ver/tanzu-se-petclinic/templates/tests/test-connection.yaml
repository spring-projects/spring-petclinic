apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tanzu-se-petclinic.fullname" . }}-test-connection"
  labels:
    {{- include "tanzu-se-petclinic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tanzu-se-petclinic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
