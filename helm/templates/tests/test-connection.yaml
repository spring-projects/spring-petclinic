apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-petclinic-helm.fullname" . }}-test-connection"
  labels:
    {{- include "spring-petclinic-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-petclinic-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
